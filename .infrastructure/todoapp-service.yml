apiVersion: v1
kind: Service
metadata:
  name: todoapp
  namespace: todoapp
spec:
  selector:
    app: todoapp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort
